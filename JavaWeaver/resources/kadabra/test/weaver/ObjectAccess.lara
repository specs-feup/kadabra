import weaver.Query;

aspectdef ObjectAccessTest

	//println("AST:\n" + Query.root().ast);

	//println("Code:\n" + Query.root().code);

	println("Call: " + Query.search("method").search("call").first().code);



	//println("Arg arg ast: " + Query.search("method").search("call").first().arguments[0].ast);
	println("Arg type type: " + Query.search("method", {name: "test"}).search("call").first().arguments[0].typeReference);
	println("Arg type package: " + Query.search("method", {name: "test"}).search("call").first().arguments[0].typeReference.package);	


//	for(var $expr of Query.search("expression", {code: /^R\.*/})) {
//		println("Code -> " + $expr.code + ": " + $expr.type);
//		println("Qualified name -> " + $expr.qualifiedName);
//	}

/*
	select class.call end
	apply
		println("Call: " + $call.code);
	end
*/
end
