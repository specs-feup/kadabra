import weaver.WeaverJps;

aspectdef Type

	for(var $var of WeaverJps.search("var")) {
		println("Var type: " + $var.type);
	}

		//var $insertedJp = $var.insertBefore(%{if(true == true) {throw new RuntimeException("TEST");}}%);
end
